import type { Metadata } from "next";

import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata: Metadata = {
  title: "Organización",
};

export default function OrganizacionPage() {
  return <PagePlaceholder title="Organización" />;
}
