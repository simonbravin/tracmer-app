"use client";

export function AppShellFooter() {
  return (
    <footer className="flex shrink-0 items-center justify-between gap-2 border-t border-border/70 bg-background px-4 py-2 text-xs text-muted-foreground md:px-5">
      <span className="min-w-0 truncate">Tracmer · panel administrativo</span>
      <span className="hidden shrink-0 sm:inline">⌘K / Ctrl+K</span>
    </footer>
  );
}
